import React from 'react';
import { Button, Container } from '../components';

const Main = () => {
  return (
    <Container>
      <Button.Primary label="Holi Holi" onPress={() => {}} />
    </Container>
  );
};

export default Main;
